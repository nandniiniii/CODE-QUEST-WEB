# 🚀 Code Quest Arena — Web Edition

An AI-powered web version of Code Quest Arena. Every quiz question is **freshly generated by Gemini AI** — no two playthroughs are ever the same — and a built-in AI tutor, **CodeBot**, is available to explain any answer in plain language.

> 🎮 Console version: see [CodeQuestArena](https://github.com/nandniiniii/CodeQuestArena) for the original Java/OOP build this project evolved from.

## ✨ What's New vs the Console Version

| Console Version | Web Version |
|---|---|
| Java, runs locally | React + Flask, runs in the browser |
| Fixed bank of 56 questions | AI generates unlimited fresh questions |
| No hints | CodeBot AI tutor explains every answer |
| Local high score file | Live, hosted, shareable link |

## 🗂️ Project Structure

```
CodeQuestArena-Web/
├── backend/
│   ├── app.py                  # Flask server + API routes
│   ├── question_generator.py   # Gemini-powered question generation
│   ├── chat_tutor.py           # CodeBot AI tutor logic
│   ├── fallback_questions.py   # Backup questions if Gemini is down
│   ├── requirements.txt
│   └── .env.example
├── frontend/
│   ├── src/
│   │   ├── components/
│   │   │   ├── WelcomeScreen.jsx
│   │   │   ├── CategorySelector.jsx
│   │   │   ├── QuizCard.jsx
│   │   │   ├── ChatTutor.jsx
│   │   │   └── ScoreBoard.jsx
│   │   ├── api.js
│   │   ├── App.jsx
│   │   ├── main.jsx
│   │   └── index.css
│   ├── index.html
│   ├── package.json
│   ├── vite.config.js
│   ├── tailwind.config.js
│   └── .env.example
└── README.md
```

## 🤖 How the AI Integration Works

**Dynamic Question Generation** — Every time a question loads, the frontend calls `/api/generate-question` with the chosen category and difficulty. The backend prompts Gemini to write a brand-new question, validates the JSON response (correct answer must match one of the options), and returns it. If Gemini fails or returns something malformed, the backend automatically falls back to a hardcoded question bank so the quiz never breaks.

**CodeBot Tutor** — After answering, players can open the CodeBot chat panel. Each message sent includes the current question's context (the question text, correct answer, and what the student answered) so CodeBot's explanations are always relevant to what's on screen, not generic.


## 🚧 Future Enhancements

- Difficulty that auto-adapts based on running accuracy
- Persistent leaderboard using a real database (MongoDB Atlas, same as Eco-Classify)
- User accounts so players can track progress across sessions
- Voice input for answering questions
- Multiplayer "quiz battle" mode

## 📄 License

Free to use and modify for learning and portfolio purposes.

---

Built by Nandni Singh — React + Tailwind + Flask + Gemini AI
